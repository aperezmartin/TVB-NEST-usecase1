Done:
------
- python3 main.py --gerneral_-settings(the same) --action_plan(MODIFIED)
- XML actions calls independent Adapter file
- Every Adapter call Mock Application file with Parameters included

NOTE: parameters, interscalehub, use case(adapter, plan and actions) have different base root, careful with that!!

ToDo:
-----
- Mock App file does the MPI calls


